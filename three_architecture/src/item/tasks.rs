pub struct Task {
    name: String,
    user_id: i32,
    term: String,
}

pub fn registerTasks(task: Task) -> boolean {
    // ユーザーを取得する
    // 「活性」ステータスのユーザーのみ担当に設定できる
    // DBリポジトリを呼ぶ
    // Resultの分岐処理を行う
    true
}
